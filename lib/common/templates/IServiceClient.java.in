package de.rwth_aachen.awap.service.remote;

public interface I{{ service.name }}Client {
	public void serviceFound(byte listenerId, {{ service.name }} service);
	public void serviceRemoved(byte listenerId, {{ service.name }} service);
%% for msg in service.messages
	%# Remember, that tx and rx are reversed, now that we are the client!
	%% if msg.tx
	public void onReceive({{ service.name }}.{{ msg.name }} msg);
	%% endif
%% endfor
}
